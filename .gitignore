node_modules/
build/
dist/
coverage/
.DS_Store
*.log
.env*
!.env.example

# Playwright artifacts
test-results/
playwright-report/

# Vault sync marker — local only, managed by /fechar-sessao
.claude/last-vault-sync

# Supabase CLI cache local — nunca versionar
supabase/.temp/
